---
title: Slider
description: منزلق رقمي يدعم السحب والضبط الدقيق بلوحة المفاتيح، ويُعرض على لوحة pingo.
---

# Slider

يُستخدم المنزلق لاختيار قيمة ضمن نطاق. تُعرض المعاينة أدناه مباشرة عبر محرك pingo — يمكنك سحب المقبض أو الضبط الدقيق بمفاتيح الأسهم، وتتبع المظهر الفاتح والداكن للموقع.

:::preview slider-basic
:::

## الاستخدام

```tsx
import { createElement } from "@dopejs/pingo";
import { Slider } from "@dopejs/pingo-ui";

root.render(
  createElement(Slider, {
    defaultValue: 40,
    min: 0,
    max: 100,
    step: 1,
    semanticLabel: "مستوى الصوت",
    onValueChange: (value) => console.log(value),
  }),
);
```

يحتفظ `Slider` داخليًا بحالة السحب عبر hooks، ويجب تركيبه كمكوّن باستخدام `createElement`. عند تمرير `value` يدخل في الوضع المُتحكَّم به؛ وإلا فاستخدم `defaultValue` ليتولى المكوّن الحالة بنفسه.

## أمثلة

### النطاق والخطوة

يحدد `min` / `max` نطاق القيم المسموح بها (الافتراضي 0–100)، ويحدد `step` دقة الضبط بلوحة المفاتيح (الافتراضي 1).

### التعطيل

عند تمرير `disabled` يتوقف المنزلق عن الاستجابة للسحب ولوحة المفاتيح.

## Props

| Prop | النوع | القيمة الافتراضية | الوصف |
| --- | --- | --- | --- |
| `value` | `number` | — | القيمة الحالية المُتحكَّم بها |
| `defaultValue` | `number` | `min` | القيمة الابتدائية غير المُتحكَّم بها |
| `onValueChange` | `(value: number) => void` | — | استدعاء عند تغيّر القيمة |
| `min` | `number` | `0` | القيمة الدنيا |
| `max` | `number` | `100` | القيمة القصوى |
| `step` | `number` | `1` | خطوة لوحة المفاتيح |
| `disabled` | `boolean` | `false` | حالة التعطيل |
| `semanticLabel` | `string` | — | اسم إمكانية الوصول |
| `className` | `string` | — | يُضاف بعد اسم فئة المكوّن |

## إمكانية الوصول

يحمل المكوّن الدور الدلالي `slider`، وقيمته الدلالية هي الصيغة النصية للقيمة الحالية. `←`/`↓` تنقص `step` واحدًا، و`→`/`↑` تزيد `step` واحدًا، وينقل `Home`/`End` إلى طرفي النطاق؛ وتُقيَّد القيمة دائمًا ضمن `[min, max]`.
